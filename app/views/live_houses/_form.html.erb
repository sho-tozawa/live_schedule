<%= form_with(model: live_house, local: true) do |form| %>
  <% if live_house.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(live_house.errors.count, "error") %> prohibited this live_house from being saved:</h2>

      <ul>
      <% live_house.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
